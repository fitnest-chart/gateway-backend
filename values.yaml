# Default values for api-gateway
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
image:
  repository: ghcr.io/fitnest-backend/api-gateway
  tag: "d3389f9"
  pullPolicy: IfNotPresent
  imagePullSecrets:
    - name: ghcr-secret
service:
  type: ClusterIP
  port: 80
  targetPort: 8080
resources:
  limits:
    cpu: 500m
    memory: 1Gi
  requests:
    cpu: 250m
    memory: 512Mi
env:
  JWT_SECRET: "your-jwt-secret-change-in-production"
  JWT_ISSUER: "iam-service"
  REDIS_HOST: "redis"
  REDIS_PORT: "6379"
  REDIS_TIMEOUT: "2000ms"
  IAM_SERVICE_URL: "http://iam-service:8080"
  MEDIA_SERVICE_URL: "http://media-service:8086"
istio:
  enabled: true
  pathPrefix: ""
  gateway:
    create: true
    name: public-gateway
    host: "*"
    port: 80
    selector:
      istio: ingressgateway
  virtualService:
    create: true
    hosts:
      - "*"
    corsPolicy:
      allowOrigins:
        - exact: "*"
      allowMethods:
        - POST
        - GET
        - OPTIONS
        - PUT
        - DELETE
      allowHeaders:
        - authorization
        - content-type
        - keep-alive
        - user-agent
        - cache-control
        - content-transfer-encoding
        - x-accept-content-transfer-encoding
        - x-accept-response-streaming
        - x-user-agent
        - x-grpc-web
      maxAge: 100s
    http:
      # Service API Docs Routes
      - match:
          - uri:
              prefix: /v3/api-docs/iam-service
        rewrite:
          uri: /v3/api-docs
        route:
          - destination:
              host: iam-service
              port:
                number: 8080
      - match:
          - uri:
              prefix: /v3/api-docs/media-service
        rewrite:
          uri: /v3/api-docs
        route:
          - destination:
              host: media-service
              port:
                number: 8086
      - match:
          - uri:
              prefix: /v3/api-docs/catalog-service
        rewrite:
          uri: /v3/api-docs
        route:
          - destination:
              host: catalog-service
              port:
                number: 8080
      - match:
          - uri:
              prefix: /v3/api-docs/commerce-service
        rewrite:
          uri: /v3/api-docs
        route:
          - destination:
              host: commerce-service
              port:
                number: 8080
      - match:
          - uri:
              prefix: /v3/api-docs/fitmarket-service
        rewrite:
          uri: /v3/api-docs
        route:
          - destination:
              host: fitmarket-service
              port:
                number: 8080
      - match:
          - uri:
              prefix: /v3/api-docs/notifications-service
        rewrite:
          uri: /v3/api-docs
        route:
          - destination:
              host: notifications-service
              port:
                number: 8080
      - match:
          - uri:
              prefix: /v3/api-docs/payment-service
        rewrite:
          uri: /v3/api-docs
        route:
          - destination:
              host: payment-service
              port:
                number: 8080
      - match:
          - uri:
              prefix: /v3/api-docs/subscription-service
        rewrite:
          uri: /v3/api-docs
        route:
          - destination:
              host: subscription-service
              port:
                number: 8080
      - match:
          - uri:
              prefix: /v3/api-docs/support-service
        rewrite:
          uri: /v3/api-docs
        route:
          - destination:
              host: support-service
              port:
                number: 8080
      - match:
          - uri:
              prefix: /v3/api-docs/user-service
        rewrite:
          uri: /v3/api-docs
        route:
          - destination:
              host: user-service
              port:
                number: 8080
      # General Gateway Routes
      - match:
          - uri:
              prefix: /api/v1/
          - uri:
              prefix: /swagger-ui
          - uri:
              prefix: /v3/api-docs
          - uri:
              prefix: /webjars
          - uri:
              exact: /swagger-ui.html
        route:
          - destination:
              host: api-gateway
              port:
                number: 80
destinationRule:
  connectionPool:
    http:
      http1MaxPendingRequests: 100
      http2MaxRequests: 1000
    tcp:
      maxConnections: 100
  loadBalancer:
    simple: ROUND_ROBIN
  outlierDetection:
    consecutive5xxErrors: 5
    interval: 30s
    baseEjectionTime: 30s
    maxEjectionPercent: 50
